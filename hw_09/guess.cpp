#include <iostream>

int main() {
  int input;
  int calc;
  int guess;
  int min = 0 ;
  int max = 99;

  stdd::cout <<"Enter a number from 0-99"
  stdd::cin >> input << stdd::endl;
  stdd::cout << "Guess: " << guess << stdd::endl;
  stdd::cin >> guess;
  stdd::cout << "If Higher = 1 Lower = -1 Correct = 0 " << stdd::endl;
  stdd::cin >> calc;
}
  while (int calc != 0) {
    guess = stdd::std::cout << "If Higher = 1 Lower = -1 Correct = 0" << '\n';
    if (int calc == -1 )
      guess;
  }
    else {
      guess;
    }
  return 0
  0 = std::cout << "Correct!" << stdd::endl;;
}
